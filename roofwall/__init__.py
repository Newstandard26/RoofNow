"""roofwall — address -> roof + wall measurement report.

The package is layered so the measurement engine stays pure (geometry in,
numbers out) and can be fed by any data source (Solar API, LiDAR, drone).
"""

__version__ = "0.1.0"
